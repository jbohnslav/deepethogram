---
id: "d2a2"
status: open
deps: []
links: []
created: 2026-03-29T22:39:19Z
type: bug
priority: 1
parent: 4681
tags: [testing, ci, blocker]
---
# Fix test failures: native + Docker

Test run results from cleanup branch:

NATIVE (Mac):
- Collection FAILED — modal_gpu_test.py in repo root matched pytest discovery, imports modal which isn't in local env. No tests ran.
- Fix: exclude modal_gpu_test.py from pytest collection (add to testpaths or confcollect_ignore in pytest.ini/pyproject.toml)

DOCKER (test-gui):
- Test data downloaded OK (1.14GB)
- 2 tests passed, then 2 GUI integration tests failed
- Suite hung after second GUI failure (likely test deadlock in teardown)
- Failures appear to be real GUI test fixture issues, not just environment

TODO:
- [ ] Exclude modal_gpu_test.py from pytest collection (confcollect_ignore or move to scripts/)
- [ ] Fix the 2 failing GUI integration tests 
- [ ] Fix the test hang/deadlock after GUI test failure
- [ ] Re-run both native and Docker to verify clean
- [ ] Verify all non-GPU tests pass in both environments

## Acceptance Criteria

- [ ]

## Worklog

- [2026-03-29 18:52] — Fixed pytest collection and GUI teardown hangs.
  
  Changes:
  - Added `testpaths = tests` in `pytest.ini` so default pytest discovery no longer imports repo-root `modal_gpu_test.py`.
  - Hardened `tests/gui/conftest.py` window setup/teardown: wait for loaded project state, raise/activate exposed windows, auto-accept close prompts during tests, and stop lingering pipes/listeners before closing windows.
  
  Verification:
  - Native: `QT_QPA_PLATFORM=offscreen uv run pytest -p no:cacheprovider -v -m "not gpu"` -> 25 passed, 5 deselected.
  - Docker/Xvfb: built `deepethogram:test-gui` and ran `docker run --rm deepethogram:test-gui` -> 25 passed, 5 deselected.
  
  Commit: `42c8f00` Fix pytest collection and GUI teardown hangs.
- [2026-03-29 19:08] — Native non-GPU pytest passed on cleanup: 25 passed, 5 deselected, 5 warnings in 7.84s. Full report written to ~/.openclaw/workspace-code-deepethogram/test-results-native.md
