---
id: "81c2"
status: closed
deps: []
links: []
created: 2026-09-16T13:37:59Z
type: task
priority: 2
closed_at: 2026-09-16T13:48:08Z
resolution: completed
closed_context: codex:335ea742ef847594
assignee: codex:335ea742ef847594
---
# Honor metric direction in optional tuning and best-epoch plotting

Audit 5540: tune/feature_extractor.py and tune/sequence.py hardcode max despite configurable tune.key_metric; defaults use F1 correctly but choosing val/loss reverses ranking. viz.plot_confusion_from_logger unconditionally argmaxes key_metric, wrong for loss. Base scheduler only recognizes exact loss or SSIM: reg_loss/data_loss would be maximized, while checkpoints minimize those; L1/smoothness/sparsity would be maximized by both if selected. These are latent custom-metric paths, not additional default checkpoint failures.

## Acceptance Criteria

- [x] Define shared explicit direction for supported selected metrics and reject unsupported choices
- [x] Verify loss and score directions in affected consumers

Implementation scope: use the shared direction resolver for Ray ASHA, HyperOpt, and tune.run, with optional tune.key_metric_mode override. Read persisted direction when choosing plotting epochs and fall back to known metric names for legacy files.

## Worklog

- [2026-09-16 09:48] [codex:335ea742] — Ray ASHA, HyperOpt and run now share validated metric direction with optional tune.key_metric_mode override. Confusion plotting honors HDF5 direction metadata and supports known legacy metric names. Tests exercise both tuning entrypoints, both search modes, custom overrides, and legacy/new files. Full default suite: 81 passed, 5 GPU deselected. No live Ray or GPU job run; orchestration tested with mocked service boundaries.

## Lifecycle

- 2026-09-16T13:48:08Z [codex:335ea742ef847594] — closed (completed)
